# Absolute path to your manga library root on this server
MANGA_ROOT=/mnt/jellyfin/Manga

# Comma-separated origins allowed for CORS. Use * for LAN prototyping.
CORS_ALLOW_ORIGINS=*

# API bind options
HOST=0.0.0.0
PORT=8080
